server:
  port: 8081

spring:
  datasource:
    url: jbdc:postgresql://${STAGING_DB_HOST}:${STAGING_DB_PORT}/${STAGING_DB_NAME}?sslmode=require&ssl=true
    username: ${STAGING_DB_USERNAME}
    password: ${STAGING_DB_PASSWORD}

logging:
  level:
    root: INFO
